<template>
  <q-layout>
    <q-header style="height: 35px">
      <q-toolbar class="absolute-center justify-center myFont">
        You can’t spell awesome without ME.
      </q-toolbar>
    </q-header>

    <q-page-container>
      <router-view />
    </q-page-container>
  </q-layout>
</template>

<script>
import { useQuasar } from "quasar";
import { computed } from "vue";

export default {
  setup() {
    const $q = useQuasar();
    const buttonColor = computed(() => {
      return $q.screen.lt.md ? true : false;
    });

    return { buttonColor };
  },
};
</script>
